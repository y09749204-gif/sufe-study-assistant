# 开发预览验证记录

日期：2026-09-17。仅描述本机及 CI 已执行的检查，不代表完整安装版验收。

## 已验证

- GitHub 公共仓库 `y09749204-gif/sufe-study-assistant`，默认分支 `main`。
- 独立 PostgreSQL 测试库：17 项后端测试通过，使用虚构课程；涵盖登录鉴权、云端授权、课表去重、单双周、账号切换拒绝、无 AI 原文保存和 CUDA 失败回退逻辑。
- Node：7 项测试通过，涵盖字幕解析、公式文本、回放日期与分段下载重试。
- Web：类型检查、生产构建通过；生产依赖 npm audit 未报告漏洞。
- GitHub Actions 已通过 Linux PostgreSQL、Python、浏览器解析和前端检查。
- 打包后的 Python、PostgreSQL、API 在新临时用户目录成功启动；中文数据目录、空初始配置与鉴权 API 通过。
- Git 索引、工作区、可达历史扫描通过；源码未引入个人数据库、登录态和课程资料。
- 解包产物扫描通过（7,859 个文件），未检测到当前用户路径、常见凭据、个人数据文件或 Python 字节码。

## 仍待验收或完善

- 全新 Windows 11 用户无开发工具时的安装、卸载及中文安装路径。
- 两个真实学校账号、支持版本企微的完整本地接入。
- 真实 Ollama／兼容 API 的看图识别和复盘；真实 Whisper CPU／CUDA 音频转写（现有自动测试使用模拟模型）。
- 无 Office 的 LibreOffice 转换、磁盘不足及端口竞争。
- 数据迁移失败恢复与应用版本回退。目前实现启动前备份、事务迁移和拒绝未知 schema，尚未实现完整自动回退旧应用。
- 课表修改覆盖、跨学期管理与完整传递依赖对应源码交付。

当前 NSIS 安装包仅供受控开发验收、未签名，未发布正式 `v0.1.0`，也未上传供普通用户下载。企微无界面下载适配器明确标为未接入。

## 首次向导补充验证（0.1.0-dev.2）

- 后端共 20 项测试通过，新增草稿恢复、完成状态保护、密钥字段拒绝、低空间检测测试。
- 真实浏览器使用空白临时用户目录走完六步：草稿保存后刷新继续、确认前课程数为零、单周导入 9 节课、跳过学校连接与 AI、进入工作台后刷新不再弹出、从设置重新进入。
- 桌面宽度和 390px 窄屏检查通过；未使用真实学校账号或云端 AI。
